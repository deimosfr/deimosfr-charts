name: Check CSI Driver SMB Version

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  check-version:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Run update script
        id: update-script
        run: |
          chmod +x scripts/update-csi-driver-smb.sh
          ./scripts/update-csi-driver-smb.sh

      - name: Create Pull Request
        if: steps.update-script.outputs.update_performed == 'true'
        uses: peter-evans/create-pull-request@v6
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: "chore(csi-driver-smb): bump chart to ${{ steps.update-script.outputs.new_version }}"
          branch: chore/csi-driver-smb-version-bump
          delete-branch: true
          title: "chore(csi-driver-smb): bump chart to ${{ steps.update-script.outputs.new_version }}"
          body: |
            Bumps csi-driver-smb chart to `${{ steps.update-script.outputs.new_version }}`.
            
            Upstream Release: https://github.com/kubernetes-csi/csi-driver-smb/releases/tag/v${{ steps.update-script.outputs.new_version }}
          labels: |
            automation
            dependencies
